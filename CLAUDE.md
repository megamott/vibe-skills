# <название проекта>

## Языковые настройки

**Все ответы должны быть на русском языке.**

## Оркестратор скиллов

**В самом начале каждого разговора** если в проекте есть скилл оркестратор скиллов, вызывай его. Например: `skill-orchestrator`. Это обязательно.

## Обзор проекта

<название проекта> - что происходит

Ключевые функции:

1. 1
2. 2
3. 3
...

### Ключевые технологии

- 1
- 2
...

## Архитектура

Из чего состоит и каким принципам следует (Clean Architecture / Hexagonal Architecture / ...)

### Поток данных (опционально)

Заполнять если у проекта есть осмысленный data flow (бэкенд, пайплайн, обработка событий). Для библиотек/CLI/фронта может быть неприменимо.

1. Что приходит
2. Куда уходит
...

## Структура кода

Список ключевых директорий с одной строкой назначения. Дерево не пишем — оно быстро устаревает.

- `path/to/dir1/` — что здесь лежит и зачем
- `path/to/dir2/` — ...
- ...

## Настройка разработки

### Быстрый старт

1. Как установить зависимости
2. Как запустить
3. Как прогнать тесты

## Тестирование

Подробности о том, где писать и как запускать тесты.

**Команда запуска тестов:** `<команда>` (например, `pytest`, `npm test`, `go test ./...`, `just test`)

## Конфигурация

Как конфигурируется проект. Про настройки, переменные окружения и т.д.

## Внешние интерфейсы

Точки входа в проект: REST/gRPC-ручки, CLI-команды, consumer'ы очередей, публичный API библиотеки и т.д.

## Соглашения по разработке

### Качество кода

Можно что-то написать тут, но в основном должно быть в rules

### Используемые паттерны

### Формат коммитов

Пример формата — в `.claude/rules/example-commit-style.md`. Адаптируй под соглашения проекта (conventional commits, привязка к трекеру и т.д.).

## Подводные камни / Что не делать

Список вещей, на которые легко наступить и которые сложно понять из самого кода. Примеры:

- Не редактировать сгенерированные файлы в `path/to/generated/`
- Миграции БД не откатываем — пишем новые
- ...

## Типичные задачи разработки

Небольшие примеры решения задач, например как добавить миграцию БД или REST API ручку
